server:
  port: 1111
eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defualtZone: http://${eureka.instance.hostname}:${server.port}/eureka/